class UserdataModel {
  int ?contribution;
  int ?rating;
  int ? friendOfCount;
  String ? titlePhoto;
  String ?rank;
  String ? handle;
  int ? maxRating;
  String ?maxRank;
  UserdataModel({this.contribution,this.friendOfCount,this.handle,this.maxRank,this.maxRating,this.rank,this.rating,this.titlePhoto}); //
}// object
